---
title: 50 大核心句法公式与速记
description: 高考英语核心语法公式、句型判定法则与高频解题结论汇总
---

# 50 大核心句法公式与速记

本篇汇总高中英语语法中最高频的 50 条判定公式与解题结论，适用于考前系统回顾与查漏补缺。

---

## 一、句子成分与从句篇

1. **简单句核心判别式**：任何主从复合句或简单句，每个谓语动词必须有其对应的主语。句子主干有且仅有一套谓语动词（并列连词连接多谓语除外）。
2. **定语从句关系代词 vs 关系副词**：
   - 从句缺主语、宾语或定语 → 选用关系代词（that, which, who, whom, whose）。
   - 从句主干完整，仅缺状语 → 选用关系副词（where, when, why）。
3. **介词 + 关系代词**：先行词指物用 `prep. + which`，先行词指人用 `prep. + whom`。介词的选择依据从句谓语搭配、形容词搭配或先行词搭配。
4. **非限制性定语从句两大原则**：逗号之后绝对不用 `that`；指代前面整句话事实或结论时使用 `which` 或 `as`。
5. **as 与 which 引导非限制性定从的区别**：as 意为“正如”，位置灵活（句首/句中/句末），常搭配 as is known / as is expected；which 意为“这/这一点”，通常只能置于主句之后。
6. **名词性从句 that 与 what 判别式**：
   - 从句成分完整且陈述事实 → 填 `that`（不充当成分，无实义）。
   - 从句缺主语、宾语或表语，表示“所……的事物” → 填 `what`。
7. **whether 与 if 的选用界限**：在主语从句置于句首、表语从句、同位语从句、介词后宾语从句及 `whether or not` 结构中，必须使用 `whether`，不可用 `if`。
8. **表语从句因果辨析**：
   - `The reason is that...`（原因是……，不用 because）。
   - `This is because...`（这是因为……，后面跟原因）。
   - `This is why...`（这就是为什么……，后面跟结果）。
9. **同位语从句与定语从句区别**：同位语从句对前面的抽象名词（fact, idea, news, doubt）进行具体内容阐述，that 在从句中不作成分；定语从句中 that 充当主语或宾语。
10. **让步从句连词边界**：`no matter + wh-` 只能引导让步状语从句；`wh-ever` 既可引导让步状语从句，又可引导名词性从句（作主语或宾语）。

---

## 二、非谓语动词篇

11. **非谓语动词解题第一步**：先看句中是否已有谓语动词且无连词连接。若已有谓语，待填动词必为非谓语动词。
12. **非谓语逻辑主语确认**：
    - 作定语：被修饰的名词即为逻辑主语。
    - 作状语：句子的主语（逗号后面的主句主语）即为逻辑主语。
    - 作宾补：前面的宾语即为逻辑主语。
13. **非谓语语态判断**：逻辑主语主动发出动作用 doing / to do；逻辑主语承受动作（被动）用 done / to be done。
14. **非谓语时态判断**：动作未发生或表目的用 `to do`；动作与谓语同时发生或正在进行用 `doing`；动作先于谓语发生用 `having done / having been done` 或过去分词 `done`。
15. **不定式作定语的特定标志**：修饰序数词（the first, the second）、最高级（the best）、`the only / last / next` 后的名词时，必须用不定式作定语。
16. **结果状语两类分化**：
    - 意料之外的结果用 `only to do`（例如：hurried to the station, only to find the train gone）。
    - 顺其自然的结果用现在分词 `doing`（例如：The rain poured down, flooding the streets）。
17. **现在分词作状语主语一致原则**：分词动作的发出者必须与主句主语一致。若不一致，需改用状语从句或独立主格结构。
18. **with 复合结构公式**：
    - `with + 宾语 + doing`（主动/进行）
    - `with + 宾语 + done`（被动/完成）
    - `with + 宾语 + to do`（将要发生）
19. **使役动词与感官动词宾补**：
    - `see/hear sb do`（全过程）vs `see/hear sb doing`（正在进行）vs `see/hear sth done`（被动完成）。
20. **情感动词分词形容词化**：修饰事物特征用 `-ing`（exciting, inspiring）；修饰人的感受或表情用 `-ed`（excited, inspired）。

---

## 三、动词时态与语态篇

21. **时态判断三步法**：1. 看明确时间标志词；2. 无标志词看上下文基准时态；3. 判定动作先后关系。
22. **现在完成进行时标志**：`have/has been doing` 强调动作从过去某时开始一直持续到现在，且仍在进行，常搭配 for + 时间段、since 或 all morning。
23. **过去完成时成立前提**：必须以过去的某一时间点或动作作为基准（过去的过去），常搭配 `by the end of + 过去时间` 或 `by the time + 过去式从句`。
24. **将来进行时核心意义**：`will be doing` 表示在将来某一明确时刻或时间段正在发生的动作（如 this time tomorrow）。
25. **进行时被动语态**：现在进行时被动为 `is/am/are being done`；过去进行时被动为 `was/were being done`。切记不可遗漏 `being`。
26. **完成时被动语态**：现在完成时被动为 `have/has been done`；过去完成时被动为 `had been done`。切记不可遗漏 `been`。
27. **主谓一致就远原则**：主语后跟 `as well as, with, together with, along with, rather than, accompanied by, except, including` 时，谓语动词单复数由最前面的主语决定。
28. **主谓一致就近原则**：由 `either... or...`, `neither... nor...`, `not only... but also...`, `there be` 连接并列主语时，谓语动词由靠近它的主语决定。
29. **分数/百分数主谓一致**：`分数/百分数 + of + 不可数名词` 作主语，谓语用单数；`分数/百分数 + of + 复数名词` 作主语，谓语用复数。
30. **one of vs the only one of 在定从中的一致性**：
    - `one of + 复数名词 + 定语从句` → 从句谓语用复数。
    - `the only one of + 复数名词 + 定语从句` → 从句谓语用单数。

---

## 四、特殊句式与虚词篇

31. **形式主语 it 核心句型**：`It is + adj. + (for/of sb) to do sth.`（事物属性用 for，人的品格用 of）。
32. **形式宾语 it 核心公式**：`主语 + make/find/think/consider/feel + it + adj./n. + to do / that 从句`。
33. **强调句型判定法则**：去掉 `It is/was` 和 `that/who`，如果剩余部分能够组成一个结构完整、语意通顺的句子，即为强调句型。
34. **部分倒装核心触发条件**：
    - 否定副词置于句首（hardly, scarcely, seldom, rarely, never, little, by no means）。
    - `only + 状语` 置于句首（only when, only then, only in this way；注意：only 修饰主语时不倒装）。
35. **状语从句省略条件**：从句主语与主句主语一致，且从句含有 be 动词时，可同时省略从句主语与 be 动词（如 When (he was) asked, he nodded）。
36. **情态动词推测体系**：
    - 肯定推测：`must do`（一定做，针对现在）/ `must have done`（一定做过了，针对过去）。
    - 否定推测：`can't / couldn't do`（不可能做）/ `can't / couldn't have done`（不可能做过）。
    - 可能推测：`may / might do` / `may / might have done`。
37. **should have done 语义**：表示“过去本应该做某事却未做”；`shouldn't have done` 表示“过去本不该做某事却做了”。
38. **虚拟语气条件句时态倒退表**：
    - 与现在事实相反：条件句从句用过去式（be 动词用 were），主句用 `would/could/should/might + do`。
    - 与过去事实相反：从句用 `had done`，主句用 `would/could/should/might + have done`。
    - 与将来事实相反：从句用过去式 / `should do` / `were to do`，主句用 `would/could/should/might + do`。
39. **名词性从句虚拟语气（一坚持二命令三建议四要求）**：insist, order, command, suggest, advise, propose, demand, require, request, urge 后的宾语从句及同位语从句中，谓语动词使用 `(should) + do`。
40. **wish 后宾语从句虚拟**：与现在相反用过去式，与过去相反用 `had done`，与将来相反用 `would/could + do`。
